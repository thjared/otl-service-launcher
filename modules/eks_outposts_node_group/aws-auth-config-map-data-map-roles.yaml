- rolearn: ${eks_outposts_node_group_iam_role}
  username: system:node:{{EC2PrivateDNSName}}
  groups:
    - system:bootstrappers
    - system:nodes
%{ if admin_role_arn != "" ~}
- rolearn: ${admin_role_arn}
  username: admin
  groups:
    - system:masters
%{ endif ~}
